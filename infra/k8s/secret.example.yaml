# Copy and replace the example value; this file is deliberately outside Kustomize.
apiVersion: v1
kind: Secret
metadata:
  name: pulseguard-secrets
  namespace: pulseguard
type: Opaque
stringData:
  ingest-api-key: REPLACE_WITH_A_RANDOM_KEY
